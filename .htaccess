# SOLUCIÓN ALTERNATIVA: Eliminar el .htaccess de la raíz
# Si el Document Root está en la raíz, este archivo NO debería existir
# O debería estar completamente vacío/comentado

# Si necesitas que la raíz redirija a public/, descomenta estas líneas:
# RewriteEngine On
# RewriteCond %{REQUEST_URI} ^/$
# RewriteRule ^$ public/index.php [L]

# IMPORTANTE: Si el Document Root puede configurarse en public/,
# elimina este archivo completamente o deja solo comentarios.
